{
  "features": [
    {
      "id": "upstream-worktree",
      "description": "Create a dedicated upstream mate_solver worktree and branch for the root stats API change.",
      "boundary": {
        "files_dirs": ["../shogi_mate_solver-worktrees/stat-emitting-feature"],
        "semantic_changes": "Create worktree only; no source edits.",
        "capability": "Run git worktree add and git status for mate_solver."
      },
      "validation": "git -C ../shogi_mate_solver-worktrees/stat-emitting-feature branch --show-current",
      "passes": true
    },
    {
      "id": "upstream-root-stats",
      "description": "Expose top-level root solve stats from mate_solver::search.",
      "boundary": {
        "files_dirs": ["../shogi_mate_solver-worktrees/stat-emitting-feature/mate_solver/src/lib.rs"],
        "semantic_changes": "Add root stats types and collect df-pn/eval positions inspected for the top-level root solve only.",
        "capability": "Edit file; run cargo fmt and cargo test in the upstream worktree."
      },
      "validation": "cargo test",
      "passes": true
    },
    {
      "id": "upstream-pr",
      "description": "Commit, push, and open an upstream mate_solver PR for the root stats API change.",
      "boundary": {
        "files_dirs": ["../shogi_mate_solver-worktrees/stat-emitting-feature"],
        "semantic_changes": "Publish the upstream root stats API change for review without modifying web solver files.",
        "capability": "Run git status, git add, git commit, git push, and gh pr create/view for mate_solver."
      },
      "validation": "gh pr view",
      "passes": true
    },
    {
      "id": "web-rust-stats-payload",
      "description": "Serialize upstream root stats through the wasm Answer payload.",
      "boundary": {
        "files_dirs": ["Cargo.toml", "Cargo.lock", "src/lib.rs"],
        "semantic_changes": "Consume the updated mate_solver API and add namespaced string-valued stats to the wasm answer JSON.",
        "capability": "Edit files; run cargo test and dependency update commands needed for mate_solver."
      },
      "validation": "cargo test",
      "passes": true
    },
    {
      "id": "web-types-stats",
      "description": "Update TypeScript Answer types to include namespaced string-valued stats.",
      "boundary": {
        "files_dirs": ["www/src/branches.ts"],
        "semantic_changes": "Add Stats, DfPnStats, EvalStats, and Answer.stats type fields with string positions_inspected.",
        "capability": "Edit file; run frontend lint, prettier check, and build."
      },
      "validation": "cd www && npm run lint && npm run test:prettier && npm run build",
      "passes": true
    }
  ]
}
